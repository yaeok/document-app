class Memo {
  int id;
  String content;
  DateTime createdAt;
  DateTime updatedAt;

  Memo({
    required this.id,
    required this.content,
    required this.createdAt,
    required this.updatedAt,
  });
}
